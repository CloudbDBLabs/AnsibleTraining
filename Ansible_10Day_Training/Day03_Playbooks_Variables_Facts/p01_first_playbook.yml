---
- name: sample playbook-1
  hosts: localhost
  become: yes
  tasks:
    - name: task1 - execute uptime command
      command: uptime

    - name: task2 - install ksh package
      dnf:
        name: ksh
        state: present
