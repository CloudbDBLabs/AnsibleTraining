---
- name: render an application config file
  hosts: node4
  vars:
    app_name: payments
    app_env: dev
    version: "1.4.0"
    debug: TRUE
    db_host: db01
    db_port: 1521
    db_user: APP_OWNER
    db_password: "S3cr3t"
  tasks:
    - name: generate /tmp/app.conf
      template:
        src: appconfig.j2
        dest: /tmp/app.conf
